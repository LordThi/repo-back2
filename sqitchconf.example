[core]
    engine = pg
    top_dir = migration
    # plan_file = migrations/sqitch.plan
[engine "pg"]
    target = postgresql://postgres:MDP@containers-us-west-178.railway.app:6811/railway
    # registry = sqitch
    # client = psql
